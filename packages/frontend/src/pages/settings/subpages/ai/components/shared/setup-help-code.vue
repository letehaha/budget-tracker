<template>
  <!-- `break-all` keeps long URLs inside the card instead of widening it on narrow containers. -->
  <code class="bg-muted rounded px-1 py-0.5 font-mono text-xs break-all"><slot /></code>
</template>
